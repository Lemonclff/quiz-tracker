# 🖼️ 圖文設定教學 — 魔藥煉成所

> 以下對應 README 的部署步驟，附上畫面說明。

---

## 步驟 1：建立試算表

1. 瀏覽器打開 **sheets.new**
2. 看到空白試算表
3. **不需要手動輸入欄位** — GAS 後端會自動建立 `答題紀錄`（21 欄）和 `學生總分`（11 欄）兩個工作表

---

## 步驟 2：部署 Apps Script

### 2a. 打開編輯器
試算表上方選單 → **擴充功能** → **Apps Script**

### 2b. 貼上程式碼
1. 刪除編輯器內所有預設文字
2. 打開 `game/concentration-lab.html`
3. 捲到最底部，找到 `<!-- Google Apps Script 後端程式碼 -->` 區塊
4. 複製 `// ---- 設定區 ----` 到 `GAS 程式碼結束` 之間的所有程式碼
5. 在 Apps Script 編輯器貼上 (Ctrl+V)
6. 點上方 💾 儲存圖示

### 2c. 部署
1. 點右上角藍色按鈕 **部署 → 新增部署**
2. 類型選 **網頁應用程式**
3. 設定：
   - 執行身分：**我**
   - 誰可以存取：**任何人**
4. 點 **部署** 按鈕
5. 授權畫面 → 選擇你的 Google 帳號 → 允許
6. **複製出現的網址**（格式：`https://script.google.com/macros/s/xxxxx/exec`）

> ⚠️ 這個網址只會出現一次，請務必複製並存好！

### 2d. 首次授權
第一次執行時，GAS 會要求授權 `DriveApp` 和 `SpreadsheetApp` 權限。部署後請在 GAS 編輯器中手動執行一次 `doGet` 函式來觸發授權。

---

## 步驟 3：設定遊戲 HTML

1. 打開 `game/concentration-lab.html`
2. 搜尋 `WEB_APP_URL`（Ctrl+F）
3. 把網址換成步驟 2 複製的部署網址：
   ```javascript
   const WEB_APP_URL = "https://script.google.com/macros/s/YOUR_DEPLOYMENT_ID/exec";
   ```
4. 存檔

---

## 步驟 4：發佈遊戲

### 選項 A：GitHub Pages（推薦，免費永久）

```bash
cd quiz-tracker
git add .
git commit -m "update WEB_APP_URL"
git push origin main
```

### 選項 B：本地測試

直接雙擊 `concentration-lab.html` 用瀏覽器打開。測試模式會自動跳過傳輸（console 顯示 `[測試]略過傳輸`）。

---

## ✅ 驗證

1. 打開遊戲網頁，輸入班級座號 `1C32`
2. 玩幾題（點選材料 → 放入魔藥瓶 → 提交調配）
3. 回到 Google 試算表 → 重新整理 (F5)
4. 切到 `答題紀錄` 工作表 → 應看到 21 欄資料
5. 切到 `學生總分` 工作表 → 應看到自動彙整的總分

---

## 🎮 遊戲玩法說明（給學生）

```
┌──────────────────────────────────────┐
│  🧙‍♂️ 「請調配濃度 0.5 的魔藥」       │
│  ═══════════════════ [時間條 ███░]  │
├──────────────────┬───────────────────┤
│   🧪 魔藥瓶      │  📦 魔法材料架     │
│  ┌────┬────┐    │  ┌──┐┌──┐┌──┐   │
│  │ ?  │ ?  │    │  │50%││1/2││5% │   │
│  └────┴────┘    │  └──┘└──┘└──┘   │
│                 │  ┌──┐┌──┐┌──┐   │
│  📘 魔藥筆記    │  │...││...││...│   │
│  (自動出現)     │  └──┘└──┘└──┘   │
│                 │                   │
│  [🧹 倒掉重練]   │  [⚗️ 完成調配]    │
└──────────────────┴───────────────────┘
```

1. 看上方對話框的**目標濃度**（如 0.5）
2. 從右側材料架**點選等值配方**（0.5 = 50% = 1/2）
3. 材料會飛入左側魔藥瓶的槽位
4. 選好後點 **⚗️ 完成魔藥調配**
5. 系統自動計分（正確+20、速度獎勵+5、錯誤-20）
6. 通關後輸入導師密碼晉級；分數不夠可**重新挑戰**

---

## 🔄 修改遊戲後如何更新？

```bash
git add . && git commit -m "update" && git push
```

## 🎛️ 自訂關卡參數

編輯 `GAME_SETTING` 中每個關卡的設定：

| 參數 | 說明 | 範例 |
|------|------|------|
| `timeLimitSec` | 每題限時（秒） | 50 |
| `bonusTimeSec` | 速度獎勵門檻 | 10 |
| `bonusScore` | 速度獎勵分數 | 5 |
| `baseScore` | 答對基礎分 | 20 |
| `wrongPenalty` | 配方錯誤扣分 | -20 |
| `emptyPenalty` | 未放材料扣分 | -30 |
| `timeoutPenalty` | 超時扣分 | -15 |
| `hintAutoSec` | 自動提示秒數 | 15 |
| `passScore` | 通關最低分 | 60 |
| `slots` | 需選幾個材料 | 2 |

每題的 `materials` 陣列可自由增減材料，`correctAnswers` 列出所有等值配方。
